// PLC is currently monitored via WebSocket only (no REST endpoints).
// Empty placeholder kept for symmetry with cameraApi/poolApi so future
// commands (e.g. /api/plc/send-batch-status) have a clear home.

export {};
